<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.toto.mapper.BoardMapper">
	<select id="getAllFile" resultType="kr.toto.entity.Bulletin">
	 	select * from Bulletin where b_file_or_quiz = 0
	</select>
	<select id="getAllQuiz" resultType="kr.toto.entity.Bulletin">
	 	select * from Bulletin where b_file_or_quiz = 1
	</select>
	<select id="seeInBoard" resultType="kr.toto.entity.Bulletin" parameterType="kr.toto.entity.Bulletin">
		select * from Bulletin where b_num = #{b_num}
	</select>
	<select id="seeInReple" parameterType="kr.toto.entity.Bulletin" resultType="kr.toto.entity.BReple">
		select * from B_reple where b_num = #{b_num}
	</select>
	<!-- <insert id="writeBoard" parameterType="kr.toto.entity.Bulletin">
		insert into Bulletin (m_nick, b_file_or_quiz, b_title, b_content,b_word, b_file1,b_file2, b_file3, b_date, b_select) values 
		(#{m_nick),#{b_file_or_quiz},#{b_title},#{b_content},#{b_word},#{b_file1},#{b_file2},#{b_file3},now(),0)
	</insert>
	<insert id="writeParagraph" parameterType="kr.toto.entity.Paragraph">
		insert into Paragraph (m_nick, p_content, p_sentence) values (#{m_nick},#{p_content},#{p_sentence})
	</insert>
	 -->
</mapper>